# ⚙️ edu-rag-bot-gitops (ArgoCD 持续交付配置仓)

![ArgoCD](https://img.shields.io/badge/GitOps-ArgoCD-orange?logo=argo)
![Kubernetes](https://img.shields.io/badge/Kubernetes-Ready-blue?logo=kubernetes)
![Kustomize](https://img.shields.io/badge/Kustomize-Config-blue)

## 📖 仓库说明 (Repository Overview)

本仓库是 **RAG 云原生工程化实践** 项目的 **GitOps 状态配置仓库 (State Repository)**。

根据云原生 GitOps 最佳实践，本项目严格实施了**“应用源码与基础设施配置分离”**的架构。本仓库中没有任何业务代码，仅包含用于 Kubernetes 部署的声明式资源清单 (YAML)。

🔗 **主应用源码仓库请访问**: [https://github.com/AmazingYe-oss/edu-rag-bot.git]

---

## 🏗️ 交付链路机制 (Delivery Workflow)

本仓库处于整个自动化交付链路的下游，扮演着**“期望状态声明 (Desired State)”**的角色：

1. **上游触发**：主源码仓库中，GitHub Actions (CI) 完成镜像构建并推送至阿里云 ACR。
2. **自动回写**：CI 流水线最后一步使用 `github-actions[bot]` 自动修改本仓库中的 Image Tag。
3. **ArgoCD 纳管**：Kubernetes 集群内部部署的 **ArgoCD** 实时监听本仓库的变更。
4. **状态调谐 (Reconciliation)**：一旦发现本仓库的 YAML 发生漂移，ArgoCD 会自动向集群发起 `Sync` 操作，拉取最新镜像完成滚动更新。

---

## 📂 目录结构 (Directory Structure)

```text
├── apps/
│   └── edu-rag-bot/           # RAG 问答机器人 K8s 配置资源
│       ├── kustomization.yaml # Kustomize 配置文件 (自动更新镜像 Tag)
│       ├── deployment.yaml    # 服务无状态部署声明
│       ├── service.yaml       # 内部服务暴露
│       └── secret.yaml        # (可选) 凭证占位配置
└── README.md
```

---

## 🔐 安全声明 (Security Notice)

为保障云原生安全，本仓库**不包含任何明文敏感凭证 (API Keys, Passwords 等)**。
生产环境中的实际凭证通过集群内部的 Kubernetes `Secret` 结合 `envFrom.secretRef` 动态注入至容器环境变量中。

---
*Generated by GitOps Workflow.*
